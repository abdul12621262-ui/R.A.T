[build]
builder = "NIXPACKS"

[deploy]
startCommand = "./target/release/rat-signaling"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "signaling"
protocol = "TCP"
internalPort = 4899
port = 4899
