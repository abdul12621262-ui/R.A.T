# Legacy Node.js Prototype

Original AetherMod implementation: Express + Socket.IO + browser shell (Chrome `--app`).

```bash
cd legacy
npm install
npm start
```

Superseded by the Rust native stack in the repo root. Kept for protocol reference.
